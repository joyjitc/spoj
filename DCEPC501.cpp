#include<iostream>

int main(){
  ios::sync_with_stdio(0);

  int t,n;
  vector<int> price;
  cin>>t;
  while(t--)
  {
    cin>>n;
    price.resize(n);
    for(int i=0;i<n;i++)
      cin>>price[i];
    

  }
}
